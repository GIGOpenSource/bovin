{
  "name": "bovin-panel-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.mjs",
    "build:dev": "node esbuild.config.mjs --dev",
    "watch": "node esbuild.config.mjs --watch",
    "extract-i18n": "node scripts/extract-i18n.mjs",
    "generate-app-src": "node scripts/generate-app-src.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.27.4"
  }
}
